public with sharing class Test_new {
    
    public static void addCase(String i) {
        If(i=='Case'){
        case c=new case();
        c.Status='NEW';
        insert c;
        }
        System.debug('Hello world! from jenkins this is the final try');
    }
}