
public with sharing class demo1 {
    public demo1() {
        Integer i = 1000; // Beause apex is strongly typed language so we have to declare the datatype as well
        String s = 'Hello';
        System.debug(s,i);

    }
}
